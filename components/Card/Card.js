import React from 'react';
import styles from './card.module.scss';

const Card = () => {
  return (
    <section>
      <div className={styles.container}>Hola soy una card</div>
    </section>
  );
};

export default Card;
